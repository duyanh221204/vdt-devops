# nano vdt-frontend-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vdt-frontend-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/duyanh221204/vdt-frontend-config.git
    targetRevision: HEAD
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

# kubectl apply -f vdt-frontend-app.yaml
